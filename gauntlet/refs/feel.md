# feel.md — measured, not remembered

Status: **empty — waiting on reference footage.** See `timings.md` for the
footage-drop / No Mercy fallback note. Input-to-action latency is the one
number in this corpus that's hardest to get from video alone (video framerate
and input polling aren't the same clock) — treat frame-stepped estimates as
approximate and prefer emulator/gamepad captures with visible input overlays
when available.

## Input-to-action latency
- Strike: (pending)
- Grapple initiation: (pending)
- Reversal input window response: (pending)
- Getup: (pending)

This is also the one bar a capture can't fully settle on its own — see
"Play it" in the top-level verification plan. A human should play a match
on a gamepad before signing off a feel slice, not just review captures.
