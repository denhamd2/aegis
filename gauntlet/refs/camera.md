# camera.md — measured, not remembered

Status: **empty — waiting on reference footage.** See `timings.md` for the
footage-drop / No Mercy fallback note.

## FOV
- (pending)

## Height
- (pending)

## Distance (min/max, and how it scales with wrestler separation)
- (pending)

## Framing behavior
- Cut triggers (finisher, three-count, reversal): (pending)
- How the rig keeps both wrestlers in frame during grapples: (pending)

Placeholder constants currently live in `game/core/camera/match_camera.gd`
(`min_distance`, `max_distance`, `height`, `follow_speed`) — replace them
once this file has real measurements, and cite this file in the commit.
