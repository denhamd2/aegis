<?xml version="1.0" encoding="utf-8"?>
<!--
    Aegis Vault & Privacy Gallery is 100% offline.
    This manifest intentionally requests NO android.permission.INTERNET and
    NO android.permission.ACCESS_NETWORK_STATE. Do not add either permission:
    doing so would violate the app's zero-network-access design constraint.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Media library access (split by API level) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Only needed pre-scoped-storage (API 26-28) to delete media the app moves into the vault -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Vault biometric gate -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
        CAMERA is local-only: used solely to capture a single front-camera frame, on-device,
        when Intruder Alerts (Settings, opt-in) are enabled and a PIN entry fails. The photo is
        written straight into the same on-device encrypted vault storage as regular vault items
        and never touches the network — consistent with the zero-network-access design above.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />

    <application
        android:name=".AegisApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AegisVault"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AegisVault"
            android:windowSoftInputMode="adjustResize" />

        <!--
            Disguised launcher icon (Settings, opt-in): exactly one of these aliases is enabled
            at a time via PackageManager.setComponentEnabledSetting — a purely local, on-device
            PackageManager operation, not related to the app's network-access stance above.
            The default "Aegis" alias ships enabled; disguise aliases ship disabled.
        -->
        <activity-alias
            android:name=".alias.AegisLauncher"
            android:targetActivity=".MainActivity"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".alias.CalculatorLauncher"
            android:targetActivity=".MainActivity"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_calculator"
            android:roundIcon="@mipmap/ic_launcher_calculator"
            android:label="@string/app_name_calculator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".alias.NotesLauncher"
            android:targetActivity=".MainActivity"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_notes"
            android:roundIcon="@mipmap/ic_launcher_notes"
            android:label="@string/app_name_notes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
