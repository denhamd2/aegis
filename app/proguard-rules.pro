# Add project specific ProGuard rules here.
# Room, Hilt, and security-crypto ship their own consumer-proguard-rules.pro,
# so no manual keep rules are required for them here.

-keepattributes *Annotation*
-keepattributes SourceFile,LineNumberTable
